/**
 * Brute Force Solution - (Sorting Strings)
 *
 * Time Complexity  : O(n^2 log n)
 * Space Complexity : O(1)
 */
class Solution {
    public String longestCommonPrefix(String[] strings) {
        if (strings.length == 0) return "";

        if (strings.length == 1) return strings[0];

        Arrays.sort(strings);

        int n = strings.length;
        int maximumPrefixLength = Math.min(strings[0].length(), strings[n - 1].length());

        StringBuilder builder = new StringBuilder();

        for (int i = 0; i < maximumPrefixLength; i++) {
            if (strings[0].charAt(i) == strings[n - 1].charAt(i)) {
                builder.append(strings[0].charAt(i));
            }
            else {
                break;
            }
        }

        return builder.toString();
    }
}
